# Large data files — too big for git, document download instead
data/candidates.jsonl
data/candidates.jsonl.gz

# Python environment
venv/
__pycache__/
*.pyc

# OS cruft
.DS_Store
Thumbs.db

# Precomputed artifacts: embeddings.npy is tracked via Git LFS (see
# .gitattributes), NOT excluded here. features.parquet is small, committed
# normally. embedding_row_order.csv is regenerated by precompute.py, fine
# to commit too since it's tiny.
